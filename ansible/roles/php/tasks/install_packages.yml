#############################################
## Ubuntu
#############################################

- name: PHPのインストール
  become: yes
  become_method: sudo
  import_tasks:
    file: "install_debian.yml"
  when:
    - ansible_distribution in ["Ubuntu"]

#############################################
## Amazon Linux
#############################################

- name: PHPのインストール
  become: yes
  become_method: sudo
  import_tasks:
    file: "install_amazon.yml"
  when:
    - ansible_distribution in ["Amazon"]